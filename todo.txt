* url to tags by name only

* tags - allow only alpha-num and dashes

* tags - need tests

* admin-post form needs cleanup - breadcrumb trail

* admin-post form hint text for Nice Url (popup maybe?)

* change password

* post.user_id should be nullable, in case of deleted user
  (should be non-null on post-creation though)
